<div class="section-meetings home-section">
  <div class="row">
    <div class="small-12 columns">
      <div class="home-title">
        <h3><%= t 'welcome.meetings.title' %></h3>
      </div>
    </div>
  </div>

  <%= cache @meetings.maximum(:updated_at) do %>
    <%= meetings_map(@meetings) %>
  <% end %>
</div>
