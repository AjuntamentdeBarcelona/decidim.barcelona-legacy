<div class="phase-nav__number">
  <span><%= step.position + 1 %></span>
</div>
<div class="phase-nav__info">
  <span class="phase-nav__title"><%= translate step.title %></span>
  <span class="phase-nav__date"><%= l step.start_at %> - <%= l step.end_at %></span>
</div>
