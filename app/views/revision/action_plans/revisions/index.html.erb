<%= link_to t('proposals.show.back_link'), revision_action_plans_url, class: 'left back' %>

<h2><%= t("revision.action_plans.index.title") %></h2>

<div class="row">
  <div class="small-8 column">
    <%= render "shared/search_form",
               search_path: revision_action_plan_revisions_path(@action_plan, page: 1),
               i18n_namespace: "revision.action_plans.index.search_form" %>
  </div>
  <div class="small-4 column">
    <%= link_to t("action_plans.index.new"), new_revision_action_plan_revision_path(@action_plan), class: 'button radius expand' %>
  </div>
</div>

<table class="clear">
  <tr>
    <th>
      <%= t("revision.action_plans.index.headers.action") %>
    </th>
  </tr>
  <% @resources.each do |revision| %>
    <tr id="<%= dom_id(revision) %>">
      <td>
        <%= revision.title %>
        <br />
        <span class="date"><%= revision.created_at %></span>
        <span class="bullet">&nbsp;&bull;&nbsp;</span>
        <%= revision.author.username %>
        <br />

        <br />

        <div>
          <%= revision.description.html_safe %>
        </div>
      </td>
    </tr>
  <% end %>
</table>

<%= paginate @resources %>
