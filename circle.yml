machine:
  services:
    - postgresql
    - redis
  ruby:
    version: 2.2.4
  environment:
    RAILS_ENV: test

test:
  pre:
    - bundle exec rake db:setup
  override:
    - bundle exec rspec:
        parallel: true
        files:
          - spec/**/*_spec.rb
